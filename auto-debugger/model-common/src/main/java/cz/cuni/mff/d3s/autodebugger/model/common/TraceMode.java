package cz.cuni.mff.d3s.autodebugger.model.common;

/**
 * Trace collection mode selection between legacy slot-based (NAIVE)
 * and event-indexed temporal tracing (TEMPORAL).
 */
public enum TraceMode {
    NAIVE,
    TEMPORAL
}
